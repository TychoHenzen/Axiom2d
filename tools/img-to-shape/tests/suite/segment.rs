#![allow(clippy::unwrap_used)]

// segment is a private module (not in pub mod list of lib.rs).
// All tests live inline in src/segment.rs: 6 tests covering Region and segment().
// This module exists to satisfy the single-binary test consolidation pattern.
