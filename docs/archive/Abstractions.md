
### Implemented Abstractions

- **Renderer trait** (`engine_render::renderer`): `clear(&mut self, color: Color)` + `draw_rect(&mut self, rect: Rect)` + `present(&mut self)` + `resize(&mut self, width, height)`. Object-safe (supports `Box<dyn Renderer>`). `NullRenderer` unit struct provides no-op impl for testing.
- **RendererRes** (`engine_render::renderer`): `#[derive(Resource)]` newtype wrapping `Box<dyn Renderer + Send + Sync>` with `Deref`/`DerefMut`. Lives in the ECS `World` as a resource — systems access it via `ResMut<RendererRes>`. Inserted by App during winit `resumed` event.
- **ClearColor** (`engine_render::clear`): `#[derive(Resource)]` wrapping `Color`. Default is cornflower blue. Paired with `clear_system(Res<ClearColor>, ResMut<RendererRes>)` which calls `renderer.clear()` with the resource's color. Apps insert `ClearColor` and register `clear_system` in `Phase::Render`.
- **HeadlessRenderer** (`engine_render::visual_regression`): GPU renderer without a window for visual regression testing. Creates wgpu device+queue with `force_fallback_adapter: true` (software rasterizer, no display needed). Renders to `Rgba8Unorm` offscreen texture with `COPY_SRC` usage. Implements full `Renderer` trait. `render_to_buffer() -> Vec<u8>` encodes a render pass, copies texture to aligned staging buffer, maps and strips row padding. Reuses shader sources and types from `wgpu_renderer` (pub(crate)). Behind `testing` feature flag.
- **ssim_compare** (`engine_render::visual_regression`): `pub fn(a: &[u8], b: &[u8], width: u32, height: u32) -> f32` — wraps `image_compare::rgba_hybrid_compare` for SSIM structural similarity. Returns 0.0–1.0 score. 0.99 threshold used for visual regression CI gate.
- **padded_row_bytes** (`engine_render::visual_regression`): `pub fn(width: u32, bytes_per_pixel: u32) -> u32` — rounds up to wgpu `COPY_BYTES_PER_ROW_ALIGNMENT` (256 bytes). Used by `render_to_buffer()`.
- **strip_row_padding** (`engine_render::visual_regression`): `pub fn(data: &[u8], width: u32, height: u32, padded_row: u32, bytes_per_pixel: u32) -> Vec<u8>` — removes wgpu readback alignment padding to produce packed pixel data. Configurable bytes-per-pixel (4 for RGBA).
- **save_golden / load_golden** (`engine_render::visual_regression`): PNG golden image I/O. `save_golden(path, pixels, w, h)` writes PNG. `load_golden(path) -> (Vec<u8>, u32, u32)` reads PNG to RGBA pixels. `GoldenError` enum for I/O and image decode failures.
- **SpyRenderer** (`engine_render::testing`): Test helper recording method calls via `Arc<Mutex<Vec<String>>>`. Builder pattern: `SpyRenderer::new(log)` then chain `.with_color_capture(arc)`, `.with_sprite_capture(arc)`, `.with_shape_capture(arc)`, `.with_blend_capture(arc)`, `.with_viewport(w, h)`. Internal `log_call()` helper deduplicates lock+push. Available behind the `testing` feature flag. Shared across crates (engine_app, demo) via dev-dependency with `features = ["testing"]`.
- **Rect** (`engine_render::rect`): `x: Pixels`, `y: Pixels`, `width: Pixels`, `height: Pixels`, `color: Color`. Derives Debug, Clone, Copy, PartialEq. Manual Default (zero-sized, WHITE color).
- **Instanced Quad Rendering** (`engine_render::wgpu_renderer`, private module): WgpuRenderer uses GPU instancing — persistent `QuadVertex` vertex buffer (4 corners of unit square) + `u16` index buffer (6 indices, 2 CCW triangles) + per-frame `Instance` buffer. `Instance { world_rect: [f32; 4], uv_rect: [f32; 4], color: [f32; 4] }` — `#[repr(C)]`, bytemuck Pod. `world_rect` encodes world coordinates `[x, y, width, height]`. `draw_rect()` and `draw_sprite()` push Instances via `rect_to_instance(rect)` (pub(crate) free fn, no viewport params). `present()` issues single `draw_indexed` call with `instance_count`. WGSL shader scales unit quad by per-instance `world_rect` (origin+size encoding), then multiplies by `camera.view_proj` to convert world→clip space. `QUAD_VERTICES` and `QUAD_INDICES` are pub(crate) constants. Default 1x1 white texture bind group; `upload_atlas(&mut self, &TextureAtlas)` replaces the bind group with atlas texture data.
- **TextureHandle** (`engine_render::atlas`): `#[derive(Debug, Clone, Copy, PartialEq)]` with `pub texture_id: TextureId` and `pub uv_rect: [f32; 4]`. Lightweight value type returned by `AtlasBuilder::add_image()`. UV rect is in normalized [0,1] coordinates.
- **AtlasError** (`engine_render::atlas`): Error enum with variants `NoSpace`, `DataLengthMismatch { expected, actual }`, `InvalidDimensions`, `DecodeError(String)`. Uses `thiserror::Error` derive.
- **AtlasBuilder** (`engine_render::atlas`): CPU-side builder using `guillotiere::AtlasAllocator` for rect packing. `new(width, height)` creates the builder. `add_image(width, height, &[u8]) -> Result<TextureHandle, AtlasError>` allocates a region and stores RGBA pixel data. `build(self) -> TextureAtlas` produces the final atlas with packed pixel buffer. Auto-increments TextureId for each added image.
- **TextureAtlas** (`engine_render::atlas`): `#[derive(Resource)]` with `data: Vec<u8>` (packed RGBA pixel buffer), `width: u32`, `height: u32`, `lookups: HashMap<TextureId, [f32; 4]>`. `lookup(TextureId) -> Option<[f32; 4]>` retrieves UV rect for a texture. Produced by `AtlasBuilder::build()`.
- **ImageData** (`engine_render::atlas`): Plain struct with `width: u32`, `height: u32`, `data: Vec<u8>`. Returned by `load_image_bytes()`.
- **load_image_bytes** (`engine_render::atlas`): `fn(&[u8]) -> Result<ImageData, AtlasError>` — wraps `image::load_from_memory`, converts to RGBA8. Supports PNG and JPEG formats.
- **normalize_uv_rect** (`engine_render::atlas`): `pub(crate) fn(x, y, w, h, atlas_w, atlas_h) -> [f32; 4]` — converts pixel-space allocation to [u0, v0, u1, v1] in [0,1] range.
- **AtlasUploaded** (`engine_render::atlas`): `#[derive(Resource)]` unit struct. Marker inserted by `upload_atlas_system` after first successful atlas upload. Prevents redundant per-frame uploads.
- **upload_atlas_system** (`engine_render::atlas`): `fn(Option<Res<TextureAtlas>>, Option<Res<AtlasUploaded>>, ResMut<RendererRes>, Commands)` — calls `renderer.upload_atlas()` when TextureAtlas is present and AtlasUploaded is absent, then inserts AtlasUploaded. Registered in `Phase::Render` (after clear_system, before camera_prepare_system).
- **blend_mode_to_blend_state** (`engine_render::wgpu_renderer`): `pub(crate) fn(BlendMode) -> wgpu::BlendState` — pure mapping: Alpha→ALPHA_BLENDING, Additive→SrcAlpha/One, Multiply→Dst/Zero. Used by pipeline creation.
- **compute_batch_ranges** (`engine_render::wgpu_renderer`): `pub(crate) fn(&[BlendMode]) -> Vec<(BlendMode, Range<u32>)>` — partitions a slice of blend modes into contiguous runs. Used by `draw_scene_to()` to issue per-batch draw calls with pipeline switching.
- **Sprite** (`engine_render::sprite`): `#[derive(Component)]` with `texture: TextureId`, `uv_rect: [f32; 4]`, `color: Color`, `width: Pixels`, `height: Pixels`. Derives Debug, Clone, Copy, PartialEq.
- **sprite_render_system** (`engine_render::sprite`): `fn(Query<(&Sprite, &GlobalTransform2D, Option<&RenderLayer>, Option<&SortOrder>, Option<&EffectiveVisibility>)>, ResMut<RendererRes>)` — filters out entities with `EffectiveVisibility(false)`, sorts by `(RenderLayer, SortOrder)` with defaults `(World, 0)`, constructs `Rect` from `GlobalTransform2D.translation` + sprite dimensions, calls `renderer.draw_sprite(rect, uv_rect)`. Registered in `Phase::Render`.
- **Plugin trait** (`engine_app::app`): `build(&self, app: &mut App)` — called eagerly inside `add_plugin()`. Plugins can register systems via `app.add_systems(Phase, system)` and insert resources via `app.world_mut()`.
- **App ECS integration** (`engine_app::app`): App owns a `World` and a `[Schedule; PHASE_COUNT]` array (one per Phase, indexed by `Phase::index()`). `handle_redraw()` iterates the array, runs each schedule, then calls `present()` on `RendererRes` from the World. Public API: `world()`, `world_mut()`, `add_systems(Phase, systems)`, `schedule_count()`. `App::new()` pre-inserts `WindowSize::default()` and `DeltaTime::default()`. `set_window_config()` and `handle_resize()` keep `WindowSize` synced.
- **WindowSize** (`engine_app::window_size`): `#[derive(Resource)]` with `width: Pixels` and `height: Pixels`. Default is 0x0. Automatically managed by App — updated on `set_window_config()` and window resize events. Systems read it via `Res<WindowSize>`.
- **Position** (`engine_core::spatial`): `#[derive(Component)]` with `x: Pixels` and `y: Pixels`. Screen-space position for entities.
- **Velocity** (`engine_core::spatial`): `#[derive(Component)]` with `dx: Pixels` and `dy: Pixels`. Per-second velocity for entities (scaled by DeltaTime in systems).
- **DeltaTime** (`engine_core::time`): `#[derive(Resource)]` newtype wrapping `Seconds`. Default is 0.0. Updated each frame by `time_system`. Systems read via `Res<DeltaTime>`. Pre-inserted by `App::new()`.
- **FixedTimestep** (`engine_core::time`): `#[derive(Resource)]` with `accumulator: Seconds` and `step_size: Seconds`. Default step_size is 1/60. `tick(delta) -> u32` returns number of fixed steps to run, retaining remainder in accumulator ("Fix Your Timestep" pattern).
- **Time trait** (`engine_core::time`): `fn delta(&mut self) -> Seconds`. Object-safe (`Send + Sync`). `SystemClock` tracks `last_instant` and returns elapsed since last call. `FakeClock` allows manual `advance(Seconds)` — `delta()` returns accumulated pending time and resets to zero.
- **ClockRes** (`engine_core::time`): `#[derive(Resource)]` newtype wrapping `Box<dyn Time>` with `Deref`/`DerefMut`. Same pattern as `RendererRes`.
- **time_system** (`engine_core::time`): `fn time_system(ResMut<ClockRes>, ResMut<DeltaTime>)` — reads delta from clock, writes to DeltaTime. Registered in `Phase::PreUpdate`.
- **ECS wrapper** (`engine_ecs`): Thin wrapper around `bevy_ecs`. Re-exports `Component`, `Resource`, `World`, `Entity`, `Query`, `Res`, `ResMut`, `Schedule`, `Commands`, `Added`, `Changed`, `With`, `Without`, `SystemSet`, `IntoScheduleConfigs`, `ScheduleSystem`. Defines `Phase` enum as schedule labels with `Phase::ALL` const array, `Phase::index() -> usize`, and `PHASE_COUNT` const.
- **InputState** (`engine_input::input_state`): `#[derive(Resource)]` with `HashSet<KeyCode>` for pressed, just_pressed, just_released. Methods: `pressed(key)`, `just_pressed(key)`, `just_released(key)`, `press(key)`, `release(key)`, `clear_frame_state()`, `action_pressed(&ActionMap, &str)`, `action_just_pressed(&ActionMap, &str)`. `press()`/`release()` allow programmatic population for testing without hardware. Action methods check if ANY bound key satisfies the condition (OR semantics).
- **ActionMap** (`engine_input::action_map`): `#[derive(Resource, Debug, Clone, Default)]` with two maps: `bindings: HashMap<String, Vec<KeyCode>>` for keyboard and `mouse_bindings: HashMap<String, Vec<MouseButton>>` for mouse. Keyboard methods: `bind(&str, Vec<KeyCode>)`, `bindings_for(&str) -> &[KeyCode]`. Mouse methods: `bind_mouse(&str, Vec<MouseButton>)`, `mouse_bindings_for(&str) -> &[MouseButton]`. Unbound actions return empty slice. Systems take `Res<ActionMap>` alongside `Res<InputState>` or `Res<MouseState>`.
- **InputEventBuffer** (`engine_input::input_event_buffer`): `#[derive(Resource)]` wrapping `Vec<(KeyCode, ElementState)>`. Staging area between winit callbacks and ECS systems. `push()` adds events, `drain()` returns a `Drain` iterator (zero-allocation). App's `handle_key_event()` pushes to this buffer if present in the World.
- **input_system** (`engine_input::input_system`): `fn input_system(ResMut<InputEventBuffer>, ResMut<InputState>)` — clears per-frame state, drains buffer, updates InputState. Registered in `Phase::Input`.
- **KeyCode** (`engine_input::prelude`): Re-export of `winit::keyboard::KeyCode`. No translation layer.
- **MouseButton** (`engine_input::prelude`): Re-export of `winit::event::MouseButton`. No translation layer.
- **MouseState** (`engine_input::mouse_state`): `#[derive(Resource)]` with `HashSet<MouseButton>` for pressed, just_pressed, just_released, plus `screen_pos: Vec2`, `world_pos: Vec2`, `scroll_delta: Vec2`. Button methods mirror InputState: `pressed(btn)`, `just_pressed(btn)`, `just_released(btn)`, `press(btn)`, `release(btn)`. Position methods: `screen_pos()`, `set_screen_pos(Vec2)`, `world_pos()`, `set_world_pos(Vec2)`. Scroll: `scroll_delta()`, `add_scroll_delta(Vec2)` (accumulator — multiple events per frame sum). `clear_frame_state()` zeroes just_pressed, just_released, and scroll_delta but NOT screen_pos/world_pos. Action methods: `action_pressed(&ActionMap, &str)`, `action_just_pressed(&ActionMap, &str)` — check ActionMap mouse bindings with OR semantics.
- **MouseEventBuffer** (`engine_input::mouse_event_buffer`): `#[derive(Resource)]` wrapping `Vec<(MouseButton, ElementState)>`. Same pattern as InputEventBuffer. App's `handle_mouse_button()` pushes to this buffer if present in the World.
- **mouse_input_system** (`engine_input::mouse_input_system`): `fn mouse_input_system(ResMut<MouseEventBuffer>, ResMut<MouseState>)` — clears per-frame state, drains buffer, updates MouseState button state. Registered in `Phase::Input`.
- **mouse_world_pos_system** (`engine_app::mouse_world_pos_system`): `fn(Res<WindowSize>, Query<&Camera2D>, ResMut<MouseState>)` — reads screen_pos from MouseState, converts to world-space via `screen_to_world()` using first Camera2D entity (or Camera2D::default() if none), writes to world_pos. Lives in engine_app (not engine_input) to avoid circular dependency with engine_render. Registered in `Phase::PostUpdate` (after visibility_system, same position as spatial_audio_system).
- **ChildOf** (`engine_scene::hierarchy`): `#[derive(Component)]` newtype wrapping `Entity`. User-facing: attach to a child entity to declare its parent. Opt-in hierarchy on flat ECS.
- **Children** (`engine_scene::hierarchy`): `#[derive(Component)]` wrapping `Vec<Entity>`. Engine-managed — never set by users directly. Derived from ChildOf by `hierarchy_maintenance_system`. Vec is sorted by Entity for deterministic traversal.
- **hierarchy_maintenance_system** (`engine_scene::hierarchy`): `fn(Query<(Entity, &ChildOf)>, Query<Entity, With<Children>>, Commands)` — rebuilds Children from ChildOf each frame. Removes stale Children when all ChildOf references to a parent are gone. Registered in `Phase::PostUpdate`.
- **SpawnChildExt** (`engine_scene::spawn_child`): Extension trait on `World`. `spawn_child(parent: Entity, bundle: impl Bundle) -> Entity` — spawns a new entity with ChildOf(parent) plus the provided bundle.
- **GlobalTransform2D** (`engine_scene::transform_propagation`): `#[derive(Component)]` newtype wrapping `Affine2`. Engine-computed world-space transform. Written by `transform_propagation_system`, never by users directly.
- **transform_propagation_system** (`engine_scene::transform_propagation`): Walks hierarchy from roots (no ChildOf) down through Children. Root: copies `Transform2D.to_affine2()` → GlobalTransform2D. Children: `parent.GlobalTransform2D * child.Transform2D.to_affine2()`. Registered in `Phase::PostUpdate` (after hierarchy_maintenance_system).
- **Visible** (`engine_scene::visibility`): `#[derive(Component)]` newtype wrapping `bool`. Default is `true`. User-facing: attach to entities to control visibility. Entities without Visible are treated as visible.
- **EffectiveVisibility** (`engine_scene::visibility`): `#[derive(Component)]` newtype wrapping `bool`. Engine-computed inherited visibility. Written by `visibility_system`, never by users directly. `parent_effective && child_visible` (AND logic).
- **visibility_system** (`engine_scene::visibility`): Walks hierarchy from roots (no ChildOf) down through Children. Root: `EffectiveVisibility = Visible` (or true if absent). Children: `parent_effective AND child_visible`. Registered in `Phase::PostUpdate` (after hierarchy_maintenance_system).
- **RenderLayer** (`engine_scene::render_order`): Enum with variants `Background`, `World`, `Characters`, `Foreground`, `UI` — declaration order IS render order via `#[derive(Ord)]`. Derives Component, Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord.
- **SortOrder** (`engine_scene::render_order`): `#[derive(Component, Default)]` newtype wrapping `i32`. Ordering within a RenderLayer. Default is 0. No clamping — negative values allowed. Used with RenderLayer as `(RenderLayer, SortOrder)` tuple sort for deterministic draw order.
- **Camera2D** (`engine_render::camera`): `#[derive(Component)]` with `position: Vec2` and `zoom: f32`. Default: position=ZERO, zoom=1.0. Camera position is the center of the view. Zoom > 1 magnifies, zoom < 1 zooms out.
- **CameraUniform** (`engine_render::camera`): `#[derive(Resource)]` with `view_proj: [[f32; 4]; 4]`. GPU-ready orthographic view-projection matrix. `CameraUniform::from_camera(camera, vw, vh)` combines view transform with Y-flipped orthographic projection for wgpu NDC.
- **camera_prepare_system** (`engine_render::camera`): `fn(Query<&Camera2D>, ResMut<RendererRes>)` — queries the first Camera2D entity (or defaults to a viewport-centered camera with zoom 1.0 if none exists), gets viewport size from renderer, computes CameraUniform, calls `renderer.set_view_projection()`. Always sets a projection — required because `rect_to_instance` passes world coordinates that need a camera matrix for correct rendering. Registered in `Phase::Render` (before sprite_render_system).
- **world_to_screen** (`engine_render::camera`): `fn(Vec2, &Camera2D, viewport_width, viewport_height) -> Vec2` — converts world-space point to screen-space pixels. Camera position maps to viewport center.
- **screen_to_world** (`engine_render::camera`): `fn(Vec2, &Camera2D, viewport_width, viewport_height) -> Vec2` — inverse of world_to_screen. Viewport center maps to camera position.
- **camera_view_rect** (`engine_render::camera`): `pub(crate) fn(&Camera2D, viewport_width, viewport_height) -> (Vec2, Vec2)` — returns (min, max) world-space AABB the camera can see. Half extents = viewport / (2 * zoom).
- **aabb_intersects_view_rect** (`engine_render::camera`): `pub(crate) fn(entity_min, entity_max, view_min, view_max) -> bool` — AABB overlap test using >= comparisons (edge-touching counts as intersecting).
- **Frustum culling in sprite_render_system**: When a Camera2D entity exists, sprites whose world AABB doesn't intersect the camera view rect are skipped. No camera → no culling (backward compatible).
- **Renderer trait** now includes `set_view_projection(&mut self, matrix: [[f32; 4]; 4])` and `viewport_size(&self) -> (u32, u32)`. WgpuRenderer uploads matrix to GPU uniform buffer; NullRenderer/SpyRenderer are no-ops.
- **GPU camera uniform**: WgpuRenderer has a persistent camera uniform buffer (bind group 1, binding 0). WGSL shader multiplies vertex position by `camera.view_proj`. `camera_prepare_system` always sets the projection each frame (viewport-centered ortho when no Camera2D exists).
- **ShapeVariant** (`engine_render::shape`): Enum with `Circle { radius: f32 }` and `Polygon { points: Vec<Vec2> }` variants. Derives Debug, Clone, PartialEq.
- **Shape** (`engine_render::shape`): `#[derive(Component)]` with `variant: ShapeVariant` and `color: Color`. Derives Debug, Clone, PartialEq.
- **TessellatedMesh** (`engine_render::shape`): Plain struct with `vertices: Vec<[f32; 2]>` and `indices: Vec<u32>`. Output of `tessellate()`.
- **tessellate** (`engine_render::shape`): `pub fn(&ShapeVariant) -> TessellatedMesh` — pure function using lyon `FillTessellator`. Circles tessellated at origin; polygons with < 3 points return empty mesh.
- **shape_aabb** (`engine_render::shape`): `pub(crate) fn(&ShapeVariant) -> (Vec2, Vec2)` — returns (min, max) AABB. Circle: `(-r, -r)` to `(r, r)`. Polygon: point extents.
- **shape_render_system** (`engine_render::shape`): `fn(Query<(&Shape, &GlobalTransform2D, Option<&RenderLayer>, Option<&SortOrder>, Option<&EffectiveVisibility>)>, Query<&Camera2D>, ResMut<RendererRes>)` — filters out `EffectiveVisibility(false)`, sorts by `(RenderLayer, SortOrder)` with defaults `(World, 0)`, tessellates each shape, offsets vertices by `GlobalTransform2D.translation`, calls `renderer.draw_shape()`. Frustum culling via shape AABB + camera view rect. No camera → no culling. Registered in `Phase::Render`.
- **Renderer trait** now includes `draw_shape(&mut self, vertices: &[[f32; 2]], indices: &[u32], color: Color)` and `apply_post_process(&mut self)`. NullRenderer/SpyRenderer implement all methods; WgpuRenderer has GPU bloom pipeline for apply_post_process and GPU shape pipeline for draw_shape.
- **ShapeVertex** (`engine_render::wgpu_renderer`): `#[repr(C)]` struct with `position: [f32; 2]` and `color: [f32; 4]`. 24 bytes, bytemuck Pod. GPU vertex format for tessellated shape rendering.
- **ShapeBatch** (`engine_render::wgpu_renderer`): Pure accumulator struct (no GPU types). `push(positions, indices, color)` appends vertices with per-vertex color and offsets indices by current vertex count for batching. `clear()` resets per frame. `is_empty()` guards against zero-length GPU buffer creation.
- **GPU Shape Pipeline** (internal to WgpuRenderer): Dedicated `SHAPE_SHADER_SRC` WGSL shader (`vs_shape`/`fs_shape`) with position+color vertex input, camera-only bind group (group 0). Separate `wgpu::RenderPipeline` from quad pipeline. Uses `u32` index format (not `u16`). Alpha blending by default. In `draw_scene_to()`, shape pass renders after quad pass within the same render pass.
- **BloomSettings** (`engine_render::bloom`): `#[derive(Resource)]` with `enabled: bool`, `threshold: f32`, `intensity: f32`, `blur_radius: u32`. Default: enabled=true, threshold=0.8, intensity=0.3, blur_radius=4. Users opt in by inserting into the World — `post_process_system` uses `Option<Res<BloomSettings>>` so it's a no-op when absent.
- **post_process_system** (`engine_render::bloom`): `fn(Option<Res<BloomSettings>>, ResMut<RendererRes>)` — calls `renderer.apply_post_process()` when BloomSettings is present and enabled. Registered in `Phase::Render` (after shape_render_system).
- **compute_gaussian_weights** (`engine_render::bloom`): `pub fn(radius: u32) -> Vec<f32>` — pure function computing normalized 1D Gaussian kernel of size 2r+1. σ = max(radius, 1). Weights sum to 1.0, symmetric.
- **FULLSCREEN_QUAD_VERTICES** (`engine_render::wgpu_renderer`): `pub(crate)` 4-vertex array covering NDC [-1,1] space for fullscreen post-process passes. Shares `QUAD_INDICES` with the scene quad.
- **GPU Bloom Pipeline** (internal to WgpuRenderer): Lazily created on first `apply_post_process()` call. Offscreen scene texture (full res) + ping/pong textures (half res). 4 render passes: brightness extraction (luminance threshold), horizontal 9-tap Gaussian blur, vertical 9-tap Gaussian blur, additive composite (scene + bloom × intensity). All intermediate textures recreated on `resize()`. Zero overhead when bloom is not active.
- **BlendMode** (`engine_render::material`): Enum with variants `Alpha`, `Additive`, `Multiply` — declaration order IS render order via `#[derive(Ord)]`. Used in `(RenderLayer, ShaderHandle, BlendMode, SortOrder)` sort for draw batching. `BlendMode::ALL: [Self; 3]` const array. `BlendMode::index(self) -> usize` for pipeline array indexing.
- **Material2d** (`engine_render::material`): `#[derive(Component)]` with `blend_mode: BlendMode`, `shader: ShaderHandle`, `textures: Vec<TextureBinding>`, `uniforms: Vec<u8>`. Default: Alpha blend, ShaderHandle(0), empty textures/uniforms. Attach to entities alongside Sprite or Shape; entities without Material2d default to Alpha blending and ShaderHandle(0). All fields are now read by render systems and forwarded to the Renderer trait.
- **ShaderHandle** (`engine_render::material`): `#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, PartialOrd, Ord)]` newtype wrapping `u32`. Returned by `ShaderRegistry::register()`. Ord enables use as sort key — shader switches are grouped as the outer key before BlendMode.
- **ShaderRegistry** (`engine_render::material`): `#[derive(Resource, Default)]` struct with `HashMap<ShaderHandle, String>` and auto-incrementing counter. `register(&mut self, source: &str) -> ShaderHandle`, `lookup(&self, handle: ShaderHandle) -> Option<&str>`. Insertable into ECS World as a Resource for system access via `Res<ShaderRegistry>`.
- **TextureBinding** (`engine_render::material`): Plain struct with `texture: TextureId` and `binding: u32`. Referenced by `Material2d::textures`. Forwarded to `renderer.bind_material_texture()` during draw calls.
- **effective_blend_mode** (`engine_render::material`): `pub fn(Option<&Material2d>) -> BlendMode` — returns `Material2d.blend_mode` or `BlendMode::Alpha` if no material present.
- **effective_shader_handle** (`engine_render::material`): `pub fn(Option<&Material2d>) -> ShaderHandle` — returns `Material2d.shader` or `ShaderHandle(0)` if no material present. Used for sort key computation and shader deduplication.
- **apply_material** (`engine_render::material`): `pub fn(renderer, material, last_shader, last_blend_mode)` — applies per-entity material state changes to the renderer with deduplication. Calls `set_shader()` and `set_blend_mode()` only when values change. Uploads uniforms (when non-empty) and texture bindings unconditionally. Shared by sprite_render_system and shape_render_system.
- **preprocess** (`engine_render::material`): `pub fn(source: &str, defines: &HashSet<&str>) -> String` — line-by-line `#ifdef`/`#endif` preprocessor for WGSL shader variant composition. Supports nested `#ifdef` blocks. Pure function, no GPU dependency.
- **Renderer trait** now includes `set_blend_mode(&mut self, mode: BlendMode)`, `set_shader(&mut self, shader: ShaderHandle)`, `set_material_uniforms(&mut self, data: &[u8])`, `bind_material_texture(&mut self, texture: TextureId, binding: u32)`, and `upload_atlas(&mut self, atlas: &TextureAtlas)`. WgpuRenderer stores 3 quad pipelines + 3 shape pipelines (one per BlendMode); `set_blend_mode()` records current mode, `draw_scene_to()` partitions draws by blend mode via `compute_batch_ranges()` and switches pipelines. `upload_atlas()` replaces the default 1×1 white texture bind group with atlas data. `set_shader()`, `set_material_uniforms()`, `bind_material_texture()` are stubs in WgpuRenderer pending GPU pipeline cache. NullRenderer no-ops all. SpyRenderer logs all; captures via `BlendCallLog`, `ShaderCallLog`, `UniformCallLog`, `TextureBindCallLog` type aliases with corresponding `with_*_capture()` builder methods.
- **sprite_render_system** now queries `Option<&Material2d>`, sorts by `(RenderLayer, ShaderHandle, BlendMode, SortOrder)`, and calls `apply_material()` which handles `set_shader()`, `set_blend_mode()`, `set_material_uniforms()`, and `bind_material_texture()` with deduplication for shader and blend mode.
- **shape_render_system** has the same Material2d integration as sprite_render_system via shared `apply_material()`.
- **PlaybackId** (`engine_audio::playback_id`): `#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]` newtype wrapping `u32`. Identifies a playing sound. Returned by `AudioBackend::play()`.
- **SoundData** (`engine_audio::sound_data`): Plain struct with `samples: Vec<f32>`, `sample_rate: u32`, `channels: u16`. Interleaved channel layout. `frame_count() -> usize` returns `samples.len() / channels`.
- **AudioBackend trait** (`engine_audio::audio_backend`): `play(&mut self, sound: &SoundData) -> PlaybackId` + `stop(&mut self, id: PlaybackId)` + `set_volume(&mut self, volume: f32)`. Object-safe (`Send + Sync`). Same hardware-abstraction pattern as Renderer trait.
- **NullAudioBackend** (`engine_audio::audio_backend`): Struct with auto-incrementing `next_id: u32`. No-op impl — `play()` increments and returns `PlaybackId`, `stop()`/`set_volume()` are no-ops. `play_count()` accessor for test assertions. Used for testing and headless mode.
- **CpalBackend** (`engine_audio::cpal_backend`): Real audio output via cpal. `Arc<Mutex<SharedState>>` holds volume, next_id counter, and `Vec<ActiveSound>` for sample mixing. `StreamHandle` wrapper makes `cpal::Stream` Send+Sync. Lazy device initialization — tests verify internal state without audio hardware. `play()` stores `SoundData` samples as `Arc<Vec<f32>>` in `ActiveSound` with cursor at 0. `stop()` removes by id. Audio callback uses `mix_into()` to additively mix all active sounds scaled by volume, advancing cursors and removing finished sounds.
- **mix_into** (`engine_audio::cpal_backend`): Pure function `fn(output: &mut [f32], state: &mut SharedState)` — zeroes output buffer, then additively mixes all active sounds scaled by volume. Advances each sound's cursor by samples consumed. Removes sounds that have been fully played. Extracted from audio callback for testability.
- **AudioRes** (`engine_audio::audio_res`): `#[derive(Resource)]` newtype wrapping `Box<dyn AudioBackend + Send + Sync>` with `Deref`/`DerefMut`. Same pattern as `RendererRes`/`ClockRes`.
- **SoundEffect** (`engine_audio::sound_effect`): Wraps `Box<dyn Fn() -> Box<dyn AudioUnit> + Send + Sync>` — factory closure producing fundsp DSP graphs. `new(factory)` takes any `Fn() -> Box<dyn AudioUnit> + Send + Sync + 'static`. `synthesize(&self, sample_rate: u32, duration: f32) -> SoundData` creates a fresh graph from the factory, sets sample rate, resets state, renders frame-by-frame via `tick()`, returns interleaved samples. Channel count derived from graph's `outputs()`.
- **SoundLibrary** (`engine_audio::sound_library`): `#[derive(Resource, Default)]` wrapping `HashMap<String, SoundEffect>`. `register(&mut self, name: &str, effect: SoundEffect)` stores by name. `get(&self, name: &str) -> Option<&SoundEffect>` looks up. Game-specific — NOT inserted by DefaultPlugins (same policy as ActionMap).
- **PlaySound** (`engine_audio::play_sound_buffer`): Plain struct with `pub name: String`. `new(impl Into<String>)` constructor. Command payload pushed into `PlaySoundBuffer`.
- **PlaySoundBuffer** (`engine_audio::play_sound_buffer`): `#[derive(Resource, Debug, Default)]` wrapping `Vec<PlaySound>`. `push(PlaySound)` enqueues, `drain() -> Drain<'_, PlaySound>` consumes. Mirrors `InputEventBuffer` pattern exactly. Inserted by DefaultPlugins (audio feature).
- **play_sound_system** (`engine_audio::play_sound_system`): `fn(ResMut<PlaySoundBuffer>, Option<Res<SoundLibrary>>, Option<Res<MixerState>>, ResMut<AudioRes>)` — drains buffer, looks up each name in SoundLibrary, synthesizes SoundData at 44100 Hz / 0.5s duration. When `MixerState` is present, forwards per-track volumes to backend via `set_track_volume()`. When `SpatialGains` are present on a command, applies mono→stereo conversion with per-channel gain scaling; culls fully-silent sounds (both gains zero). Calls `audio.play_on_track()` with the command's track. Skips unknown names silently. No-op when SoundLibrary absent. Registered in `Phase::PreUpdate` by DefaultPlugins (audio feature).
- **MixerTrack** (`engine_audio::mixer`): Enum with variants `Master`, `Music`, `Sfx`, `Ambient`, `Ui`. Derives Debug, Clone, Copy, PartialEq, Eq, Hash. `ALL: [Self; 5]` const array. `index(self) -> usize` for array indexing.
- **MixerState** (`engine_audio::mixer`): `#[derive(Resource)]` with `volumes: [f32; TRACK_COUNT]`. Default all 1.0. `track_volume(track) -> f32`, `set_track_volume(track, volume)`. No clamping. Game-specific — NOT inserted by DefaultPlugins (same policy as ActionMap).
- **AudioListener** (`engine_audio::spatial`): `#[derive(Component)]` zero-size marker struct. Attach to the entity that represents the listener (typically the camera). Only one listener is queried (via `single()`).
- **AudioEmitter** (`engine_audio::spatial`): `#[derive(Component)]` with `volume: f32` and `max_distance: f32`. Attach to entities that emit spatial sounds. Used by `spatial_audio_system` to compute stereo gains.
- **SpatialGains** (`engine_audio::spatial`): Plain struct with `left: f32` and `right: f32`. Computed by `spatial_audio_system`, consumed by `play_sound_system` for stereo panning.
- **distance_attenuation** (`engine_audio::spatial`): `pub fn(distance: f32, max_distance: f32) -> f32` — linear falloff clamped to [0, 1]. Pure function.
- **compute_pan** (`engine_audio::spatial`): `pub fn(listener_pos: Vec2, emitter_pos: Vec2) -> (f32, f32)` — constant-power stereo panning. Returns (left_gain, right_gain). Centered when emitter is ahead or coincident. Pure function.
- **compute_spatial_gains** (`engine_audio::spatial`): `pub fn(listener_pos, emitter_pos, emitter_volume, max_distance) -> SpatialGains` — combines distance_attenuation and compute_pan with emitter volume. Pure function.
- **spatial_audio_system** (`engine_audio::spatial`): `fn(Query<&GlobalTransform2D, With<AudioListener>>, Query<(&AudioEmitter, &GlobalTransform2D), Without<AudioListener>>, ResMut<PlaySoundBuffer>)` — for each PlaySound command with an `emitter` entity, computes and sets `SpatialGains` from listener/emitter world positions. No-op when no AudioListener exists. Registered in `Phase::PostUpdate` by DefaultPlugins (audio feature, chained after visibility_system).
- **AudioBackend trait** now includes `play_on_track(&mut self, sound: &SoundData, track: MixerTrack) -> PlaybackId` and `set_track_volume(&mut self, track: MixerTrack, volume: f32)`. `play()` delegates to `play_on_track()` with `MixerTrack::Sfx` as default. NullAudioBackend no-ops new methods. CpalBackend stores track volumes in SharedState; `mix_into` computes effective volume as `global_volume * track_volume`.
- **PlaySound** now has `track: MixerTrack` (default Sfx), `emitter: Option<Entity>`, `spatial_gains: Option<SpatialGains>`. Constructors: `new(name)` (Sfx, no spatial), `on_track(name, track)`, `at_emitter(name, entity)`.
- **DefaultPlugins** (`axiom2d::default_plugins`): Unit struct implementing `Plugin`. `build()` inserts `InputState`, `InputEventBuffer`, `MouseState`, `MouseEventBuffer`, `ClockRes(SystemClock)`; registers `input_system` + `mouse_input_system` (Input), `time_system` (PreUpdate), hierarchy+transform+visibility+`mouse_world_pos_system` chained (PostUpdate). When `audio` feature is on: inserts `AudioRes(NullAudioBackend)`, `PlaySoundBuffer`; registers `play_sound_system` (PreUpdate); adds `spatial_audio_system` to PostUpdate chain. When `render` feature is on: also inserts `ClearColor` and registers `clear_system`, `upload_atlas_system`, `camera_prepare_system`, `sprite_render_system`, `shape_render_system`, `post_process_system` chained (Render). Does NOT insert `ActionMap` (game-specific), `BloomSettings` (opt-in), `TextureAtlas` (game-specific), `SoundLibrary` (game-specific), or `RendererRes` (created by winit at runtime).
- **`render` feature flag** (`axiom2d`): Default-on feature. Gates `engine_render` dependency, render system registration in DefaultPlugins, and `engine_render::prelude::*` re-export. `--no-default-features` gives headless ECS-only mode.
- **`audio` feature flag** (`axiom2d`): Default-on feature. Gates `engine_audio` dependency, AudioRes insertion in DefaultPlugins, and `engine_audio::prelude::*` re-export.
- **RigidBody** (`engine_physics::rigid_body`): `#[derive(Component)]` enum with variants `Dynamic`, `Static`, `Kinematic`. Pure ECS data component describing the physics body type.
- **Collider** (`engine_physics::collider`): `#[derive(Component)]` enum with variants `Circle(f32)`, `Aabb(Vec2)`, `ConvexPolygon(Vec<Vec2>)`. Pure ECS data component describing the collision shape.
- **PhysicsBackend trait** (`engine_physics::physics_backend`): `step(&mut self, dt: Seconds)` + `add_body(&mut self, entity, body_type, position) -> bool` + `add_collider(&mut self, entity, collider) -> bool` + `remove_body(&mut self, entity)` + `body_position(&self, entity) -> Option<Vec2>` + `body_rotation(&self, entity) -> Option<f32>` + `drain_collision_events(&mut self) -> Vec<CollisionEvent>`. Object-safe (`Send + Sync`). Same pattern as Renderer and AudioBackend traits.
- **NullPhysicsBackend** (`engine_physics::physics_backend`): No-op impl with `step_count()` accessor for test assertions. Maintains `HashSet<Entity>` for registration tracking. `add_body` returns false on duplicate, `add_collider` returns true if entity is registered. `body_position`/`body_rotation` always return None. `drain_collision_events` returns empty Vec.
- **RapierBackend** (`engine_physics::rapier_backend`): Real physics via rapier2d. Wraps `PhysicsPipeline`, `RigidBodySet`, `ColliderSet`, `IslandManager`, `DefaultBroadPhase`, `NarrowPhase`, `ImpulseJointSet`, `MultibodyJointSet`, `CCDSolver`. `HashMap<Entity, RigidBodyHandle>` for ECS↔rapier correlation. `HashMap<ColliderHandle, Entity>` for reverse mapping (collider→entity) used by collision event resolution. `ChannelEventCollector` wired into `pipeline.step()` for collision event collection via crossbeam channel. `drain_collision_events()` drains the channel receiver and maps `ColliderHandle` pairs to `Entity` pairs, silently dropping events with unresolved handles. All colliders get `ActiveEvents::COLLISION_EVENTS` set unconditionally. Body type mapping: Dynamic→Dynamic, Static→Fixed, Kinematic→KinematicPositionBased. Collider mapping: Circle→ball, Aabb→cuboid, ConvexPolygon→convex_hull. Configurable gravity via `new(gravity: Vec2)`.
- **PhysicsRes** (`engine_physics::physics_res`): `#[derive(Resource)]` newtype wrapping `Box<dyn PhysicsBackend + Send + Sync>` with `Deref`/`DerefMut`. Same pattern as `RendererRes`/`AudioRes`.
- **CollisionKind** (`engine_physics::collision_event`): Enum with variants `Started`, `Stopped`. Derives Debug, Clone, Copy, PartialEq, Eq.
- **CollisionEvent** (`engine_physics::collision_event`): Plain struct with `entity_a: Entity`, `entity_b: Entity`, `kind: CollisionKind`. Derives Debug, Clone, Copy, PartialEq.
- **CollisionEventBuffer** (`engine_physics::collision_event`): `#[derive(Resource, Debug, Default)]` wrapping `Vec<CollisionEvent>`. `push(event)` enqueues, `drain() -> Drain<'_, CollisionEvent>` consumes. Same pattern as `InputEventBuffer`/`PlaySoundBuffer`. NOT inserted by DefaultPlugins (game-specific, same policy as PhysicsRes).
- **physics_step_system** (`engine_physics::physics_step_system`): `fn(Res<DeltaTime>, ResMut<PhysicsRes>, ResMut<CollisionEventBuffer>)` — calls `physics.step(dt)`, then drains `physics.drain_collision_events()` into the buffer. Designed for `Phase::PreUpdate`. NOT registered by DefaultPlugins (game-specific).
- **physics_sync_system** (`engine_physics::physics_sync_system`): `fn(Res<PhysicsRes>, Query<(Entity, &mut Transform2D), With<RigidBody>>)` — reads `body_position()` and `body_rotation()` from the physics backend for each entity with a `RigidBody` component, writes to `Transform2D.position` and `Transform2D.rotation` independently (each gated by its own `Option`). Scale is never modified. Designed for `Phase::PreUpdate` (after `physics_step_system`, before `transform_propagation_system` in PostUpdate). NOT registered by DefaultPlugins (game-specific, same policy as `physics_step_system`).

- **Serde support**: All pure-data types across the engine derive `Serialize`/`Deserialize` (serde). Core types: `Color`, `Transform2D`, `Pixels`, `Seconds`, `TextureId`, `EntityId`. Render types: `Rect`, `Sprite`, `Shape`, `ShapeVariant`, `Camera2D`, `BloomSettings`, `BlendMode`, `ShaderHandle`, `TextureBinding`, `Material2d`. Scene types: `RenderLayer`, `SortOrder`, `Visible`. Physics types: `RigidBody`, `Collider`. Audio types: `MixerTrack`, `AudioEmitter`. Workspace dependencies: `serde = { version = "1", features = ["derive"] }`, `ron = "0.9"`, glam with `serde` feature.
- **SceneNodeDef** (`engine_assets::scene`): `#[derive(Serialize, Deserialize)]` struct with `name: String`, `transform: Transform2D`, `parent_index: Option<usize>` (index-based parent-child, not Entity), and optional fields for all serializable component types (visible, render_layer, sort_order, sprite, shape, camera, rigid_body, collider, material, bloom_settings, audio_emitter). Optional fields use `#[serde(default, skip_serializing_if)]`.
- **SceneDef** (`engine_assets::scene`): `#[derive(Serialize, Deserialize)]` struct with `nodes: Vec<SceneNodeDef>`. Top-level scene container for RON serialization. Parent-child relationships via `parent_index` referencing positions in the `nodes` Vec.
- **Handle\<T\>** (`engine_assets::handle`): Generic `Copy` newtype wrapping `u32` with `PhantomData<fn() -> T>` for type safety. Unconditionally `Send + Sync` regardless of `T`. Manual impls for Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Debug (all without `T:` bounds). `pub(crate) fn new(id: u32)` constructor.
- **AssetServer\<T\>** (`engine_assets::asset_server`): `#[derive(Resource, Default)]` generic struct (bound `T: Send + Sync + 'static`) with `HashMap<u32, AssetEntry<T>>` storage and auto-incrementing `next_id: u32` counter. Per-type design: one `AssetServer<T>` per asset type in ECS World (e.g., `Res<AssetServer<SceneDef>>`). Methods: `add(T) -> Handle<T>` (stores with ref count 1), `get(Handle<T>) -> Option<&T>`, `get_mut(Handle<T>) -> Option<&mut T>`, `ref_count(Handle<T>) -> Option<usize>`, `clone_handle(Handle<T>)` (explicit ref-count increment, returns `()`), `remove(Handle<T>) -> bool` (decrement ref count, evict at zero, returns false for unknown handles), `load(&str) -> Result<Handle<T>, AssetError>` (reads RON file, deserializes via `ron::from_str`, requires `T: DeserializeOwned`).
- **AssetError** (`engine_assets::asset_server`): `#[derive(Debug, thiserror::Error)]` enum with `Io(#[from] std::io::Error)` and `Parse(#[from] ron::error::SpannedError)` variants. Returned by `AssetServer::load()`.
- **Anchor** (`engine_ui::anchor`): Enum with 9 variants: `TopLeft`, `TopCenter`, `TopRight`, `CenterLeft`, `Center`, `CenterRight`, `BottomLeft`, `BottomCenter`, `BottomRight`. Derives Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize.
- **anchor_offset** (`engine_ui::anchor`): `pub fn(Anchor, Vec2) -> Vec2` — pure function computing top-left corner offset from position based on anchor. TopLeft returns ZERO, Center returns -size/2, BottomRight returns -size, etc.
- **Margin** (`engine_ui::margin`): Plain struct with `top: f32`, `right: f32`, `bottom: f32`, `left: f32`. Default all zeros. `total_horizontal() -> f32` (left+right), `total_vertical() -> f32` (top+bottom). Derives Default, Serialize, Deserialize.
- **FlexDirection** (`engine_ui::flex_layout`): Enum with `Row`, `Column` variants. Derives Serialize, Deserialize.
- **FlexLayout** (`engine_ui::flex_layout`): `#[derive(Component)]` with `direction: FlexDirection` and `gap: f32`. Attach to parent entity with Children; `ui_layout_system` positions children sequentially.
- **compute_flex_offsets** (`engine_ui::flex_layout`): `pub fn(&FlexLayout, &[(Vec2, Margin)]) -> Vec<Vec2>` — pure function computing child position offsets. Row advances X, Column advances Y. Gap added between (not after last) children. Margin integrated into spacing.
- **UiNode** (`engine_ui::ui_node`): `#[derive(Component)]` with `size: Vec2`, `anchor: Anchor`, `margin: Margin`, `background: Option<Color>`. Default: zero size, TopLeft anchor, zero margin, no background. Derives Serialize, Deserialize.
- **ui_layout_system** (`engine_ui::layout`): `fn(Query<(&FlexLayout, &Children, &GlobalTransform2D)>, Query<(&UiNode, &mut Transform2D)>)` — reads FlexLayout + Children from parent, computes child positions via `compute_flex_offsets`, writes to child `Transform2D.position` (parent.translation + offset). Designed for Phase::Render (before ui_render_system).
- **ui_render_system** (`engine_ui::render`): `fn(Query<(&UiNode, &GlobalTransform2D, Option<&EffectiveVisibility>)>, ResMut<RendererRes>)` — filters out EffectiveVisibility(false), draws background rect via `renderer.draw_rect()` with anchor offset applied. Skips nodes with `background: None`. Designed for Phase::Render.
- **Text** (`engine_ui::text`): `#[derive(Component)]` with `content: String`, `font_size: f32`, `color: Color`. Default: empty string, 16.0, WHITE. Data-only for now — bitmap font glyph rendering deferred. Derives Serialize, Deserialize.
- **RectCallLog** (`engine_render::testing`): `Arc<Mutex<Vec<Rect>>>` type alias. SpyRenderer `with_rect_capture()` builder method captures Rect values from `draw_rect()` calls.
- **Interaction** (`engine_ui::interaction`): `#[derive(Component)]` enum with `None` (default), `Hovered`, `Pressed`. Engine-computed by `ui_interaction_system`. Derives Serialize/Deserialize.
- **ui_interaction_system** (`engine_ui::interaction`): `fn(Res<MouseState>, Query<(Entity, &UiNode, &GlobalTransform2D, &mut Interaction, Option<&EffectiveVisibility>, Option<&Button>)>, ResMut<UiEventBuffer>, ResMut<FocusState>)` — AABB hit-testing using `anchor_offset` + `MouseState.world_pos()`, writes Interaction state, emits UiEvents on transitions, manages focus on click. Skips `EffectiveVisibility(false)` and `Button { disabled: true }` entities. Designed for Phase::PostUpdate (after `mouse_world_pos_system`).
- **FocusState** (`engine_ui::interaction`): `#[derive(Resource)]` with `focused: Option<Entity>`. Tracks which UI entity has keyboard focus. Updated by `ui_interaction_system` on click.
- **UiEvent** (`engine_ui::ui_event`): Enum with `Clicked(Entity)`, `HoverEnter(Entity)`, `HoverExit(Entity)`, `FocusGained(Entity)`, `FocusLost(Entity)`. Not serializable (Entity is not serde-compatible).
- **UiEventBuffer** (`engine_ui::ui_event`): `#[derive(Resource, Default)]` wrapping `Vec<UiEvent>`. `push(event)` enqueues, `drain() -> Drain<'_, UiEvent>` consumes. Same pattern as `InputEventBuffer`/`PlaySoundBuffer`.
- **Button** (`engine_ui::button`): `#[derive(Component)]` with `disabled: bool`. Marker component for button widgets. `button_render_system` draws using `UiTheme` colors based on `Interaction` state (disabled overrides all). Derives Serialize/Deserialize.
- **button_render_system** (`engine_ui::button`): `fn(Query<(&Button, &UiNode, &GlobalTransform2D, Option<&Interaction>, Option<&EffectiveVisibility>)>, Res<UiTheme>, ResMut<RendererRes>)` — draws button background rect with color from UiTheme based on Interaction (disabled > pressed > hovered > normal). Designed for Phase::Render.
- **Panel** (`engine_ui::panel`): `#[derive(Component)]` with `border_color: Option<Color>` and `border_width: f32`. `panel_render_system` draws UiNode background + 4-edge border rects when border_color is Some. Derives Serialize/Deserialize.
- **panel_render_system** (`engine_ui::panel`): `fn(Query<(&Panel, &UiNode, &GlobalTransform2D, Option<&EffectiveVisibility>)>, ResMut<RendererRes>)` — draws background from UiNode.background, then 4 border edge rects (top/bottom/left/right) when Panel.border_color is present. Designed for Phase::Render.
- **ProgressBar** (`engine_ui::progress_bar`): `#[derive(Component)]` with `value: f32` and `max: f32`. Default: value=0, max=100. No clamping on stored fields. Derives Serialize/Deserialize.
- **progress_bar_render_system** (`engine_ui::progress_bar`): `fn(Query<(&ProgressBar, &UiNode, &GlobalTransform2D, Option<&EffectiveVisibility>)>, Res<UiTheme>, ResMut<RendererRes>)` — draws UiNode background, then filled rect with width = `(value/max).clamp(0,1) * node.size.x` using `UiTheme.normal_color`. Designed for Phase::Render.
- **UiTheme** (`engine_ui::theme`): `#[derive(Resource)]` with `normal_color`, `hovered_color`, `pressed_color`, `disabled_color` (all Color), `text_color: Color`, `font_size: f32`. Opt-in — NOT inserted by DefaultPlugins (same policy as BloomSettings). Derives Serialize/Deserialize.
