name: CI

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  rust:
    name: Rust workspaces
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87 # stable
        with:
          toolchain: 1.94.0
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libasound2-dev
      - name: Check formatting
        run: |
          cargo fmt --all --check
          cargo fmt --manifest-path tools/Cargo.toml --all --check
      - name: Run Clippy
        run: |
          cargo clippy --workspace --all-targets --keep-going --locked -- -D warnings
          cargo clippy --manifest-path tools/Cargo.toml --workspace --all-targets --keep-going --locked -- -D warnings
      - name: Build and test engine workspace
        run: |
          cargo build --workspace --locked
          cargo test --workspace --locked
      - name: Build and test tools workspace
        run: |
          cargo build --manifest-path tools/Cargo.toml --workspace --locked
          cargo test --manifest-path tools/Cargo.toml --workspace --locked

  audit:
    name: Cargo audit
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87 # Rust 1.94.0
        with:
          toolchain: 1.94.0
      - uses: actions-rust-lang/audit@72c09e02f132669d52284a3323acdb503cfc1a24 # v1.2.7
        with:
          file: Cargo.lock
          # Linux-only wayland-scanner still brings quick-xml below 0.41 transitively.
          # Keep these two IDs scoped to that upstream path until it publishes a fix.
          ignore: RUSTSEC-2026-0194,RUSTSEC-2026-0195
          createIssues: "false"
      - uses: actions-rust-lang/audit@72c09e02f132669d52284a3323acdb503cfc1a24 # v1.2.7
        with:
          file: tools/Cargo.lock
          # wayland-scanner and zbus_xml pin the affected quick-xml versions transitively.
          # The tools workspace's direct quick-xml dependency is already on 0.41.
          ignore: RUSTSEC-2026-0194,RUSTSEC-2026-0195
          createIssues: "false"

  actionlint:
    name: actionlint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Check workflow syntax and embedded shell
        run: go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.12 .github/workflows/*.yml

  visual-regression:
    name: Visual regression
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87 # Rust 1.94.0
        with:
          toolchain: 1.94.0
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
      - name: Install system and software GPU dependencies
        run: sudo apt-get update && sudo apt-get install -y libasound2-dev mesa-vulkan-drivers
      - name: Run visual regression tests
        env:
          WGPU_BACKEND: vulkan
        run: cargo test -p engine_render --test main --locked -- --test-threads=1
